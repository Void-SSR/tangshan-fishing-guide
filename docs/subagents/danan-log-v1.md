# 大南工作日志 v1

## 作用

记录大南的持续工作记忆。线程失效时，新接任者先看这里，再继续工作。

## 当前角色拥有者

- 当前优先拥有者：`Feynman`
- 当前 Agent ID：`019d8a1e-2845-7c63-a523-79c71626e15e`
- 上一任：无

## 当前主责任文件

- [qa-and-ux-checklist-v1.md](/Users/ssr/Downloads/Openai%20Codex/tangshan-fishing-guide/docs/qa-and-ux-checklist-v1.md:1)
- [smoke-static.mjs](/Users/ssr/Downloads/Openai%20Codex/tangshan-fishing-guide/site/tests/smoke-static.mjs:1)
- [smoke.spec.mjs](/Users/ssr/Downloads/Openai%20Codex/tangshan-fishing-guide/site/tests/smoke.spec.mjs:1)

## 日志格式

- 日期
- 本轮目标
- 已完成
- 已吸收结论
- 未解风险 / 下一步

## 已有记录

### 2026-04-14 第一轮

- 本轮目标：建立当前环境可执行的质量门槛
- 已完成：
  - 建立静态 smoke 思路
  - 明确三层结构红线、危险提示规则和状态恢复是高风险点
- 已吸收结论：
  - 当前环境跑不了本地端口时，先用静态 smoke 顶住主线
- 未解风险 / 下一步：
  - 浏览器 E2E 仍需保留脚本，等待环境允许后接上

### 2026-04-14 第二轮

- 本轮目标：把最容易回退的点做成静态断言
- 已完成：
  - 增加排序、危险提示、相关推荐、来源文案、`undefined` 泄漏断言
  - 增加详情页结构和核验面板断言
- 已吸收结论：
  - 不只要测样例页，要测全量详情页
- 未解风险 / 下一步：
  - 真图替换后还需要加图片一致性保护

### 2026-04-15 第三轮

- 本轮目标：盯真图替换后的质量风险
- 已完成：
  - 补了卡片串图、空 `src`、空 `alt` 保护
  - 补了核验与图片说明面板的浏览器侧脚本
- 已吸收结论：
  - 图片替换最大的风险不是“页面打不开”，而是“图串了、状态文案错了”
- 未解风险 / 下一步：
  - 环境允许后要真实跑一次浏览器 E2E
  - 真图继续替换时，要继续盯 `remote_asset` 与文案的一致性

### 2026-04-15 第四轮

- 本轮目标：验证第一版站点的本地直接浏览链路，并复查三层结构红线
- 已完成：
  - 主线程代行大南职责执行一轮浏览式验收
  - 确认当前环境无法用 `file://` 打开 Playwright 页面，也无法绑定本地端口做真实浏览器访问
  - 将站点从根路径资源改为相对路径资源，支持直接打开本地 `index.html`
  - 为 smoke 增加“禁止残留根路径内部资源”的断言
  - 重新构建并通过 `npm run test:smoke`
- 已吸收结论：
  - 当前环境下，真实浏览器式本地访问受沙箱限制，不能作为唯一验收手段
  - 必须同时保证两条链路：部署链路可用、本地双击 HTML 也可浏览
  - 本地可浏览性的最低门槛，是页面内部不能残留 `href=\"/...\"` 或 `src=\"/...\"`
- 未解风险 / 下一步：
  - 环境允许后仍要补真实浏览器 E2E
  - 真图替换后继续盯相对路径兼容，不要把根路径资源重新带回页面

### 2026-04-15 第五轮

- 本轮目标：解决“大南无法稳定浏览验站”和“主线程无法直接开网站”的机制问题
- 已完成：
  - 确认当前环境里大南原拥有者不在可用子线程列表中，按协议切换为“可接任则接任、不可接任则主线程代行”
  - 新增宿主机预览脚本 `site/preview-local.command`
  - 新增预览环境诊断脚本 `site/scripts/check-preview-env.mjs`
  - 确认当前环境诊断结果为：本地端口绑定 `EPERM`，因此不能把沙箱里无法开浏览器误判为网站空白
  - 更新大南职责和复用协议，固定浏览 / QA 的降级顺序
- 已吸收结论：
  - 当前问题的主因是环境权限，不是网站文件本身无内容
  - 大南测试能力不能只依赖活跃线程，必须有“主线程代行 + 角色日志续接”的兜底
  - 预览能力必须拆成两条：宿主机真实预览链路、沙箱内静态 smoke 验收链路
- 未解风险 / 下一步：
  - 环境允许时仍要恢复真实浏览器 E2E
  - 后续如需聊天内可视化预览，还要补旁路预览层或截图链路

### 2026-04-16 第六轮

- 本轮目标：修正 GitHub Pages 发布后首页空壳风险，并把“大南先验收再交付”固化成强制门槛
- 已完成：
  - 确认阶段性交付前必须先经过大南 QA Gate，并写入复用与交接协议
  - 识别出 GitHub Pages 下“不带尾斜杠访问”会让相对路径资源失效的兼容问题
  - 为所有页面增加动态 `<base>` 注入，兼容 `file://`、GitHub Pages 子路径、以及不带尾斜杠访问
  - 识别出 Service Worker 缓存名仅按日期生成，会导致同一天重复发布时命中旧壳缓存
  - 将 Service Worker 缓存名改为带秒级时间戳的构建标识
  - 顺序重跑 `npm run test:smoke` 并通过
- 已吸收结论：
  - GitHub Pages 成功部署不等于用户一定看到最新页面，路径兼容和缓存版本同样是发布门槛
  - 发布后出现“页面几乎空白”时，必须优先排查：路径基准、Service Worker 缓存、而不是先怀疑数据丢失
  - QA Gate 不能只看 workflow 变绿，必须把用户实际访问路径纳入验收
- 未解风险 / 下一步：
  - 需要把这轮修复推送到 GitHub，再触发 Pages 重建
  - 推送完成后，继续观察首页是否仍受旧 Service Worker 影响；必要时再加显式缓存更新提示

### 2026-04-16 第七轮

- 本轮目标：把当前版本收口成正式可查验版本
- 已完成：
  - 复核 `npm run test:smoke` 通过
  - 复核首页、列表页、详情页构建结果均包含动态 `<base>` 兼容层
  - 补齐“先安全预检，再大南验收，再交付”的正式版本门槛
  - 更新第一版交付说明和发布清单到当前真实状态
- 已吸收结论：
  - 正式版本不只看功能完成，还要把发布条件、验收门槛和当前限制写清楚
  - GitHub Pages 可访问不等于版本可交付，交付条件仍要显式落文档
- 未解风险 / 下一步：
  - 需要把当前本地新提交继续推到远端
  - 推送后重新触发 Pages 构建，确认线上已拿到这轮路径与缓存修复

### 2026-04-16 第八轮

- 本轮目标：解决线上已成功部署但用户仍命中旧空壳的问题
- 已完成：
  - 判断当前主因不是 GitHub Pages workflow，而是旧 Service Worker / 旧缓存仍在控制页面
  - 将页面入口资源改为带构建时间戳的查询参数，强制浏览器获取新 `app.js` 和新 `site.css`
  - 将运行时逻辑改为：页面加载后主动注销既有 Service Worker，并清理 `tangshan-fishing-guide-*` 缓存
  - 重跑 `npm run safety:preflight`
  - 重跑 `npm run test:smoke`
- 已吸收结论：
  - 仅仅修改缓存名还不够；如果旧 Service Worker 先拦住入口脚本，用户仍可能留在旧壳
  - 线上问题要优先拆成“部署是否成功”和“客户端是否还受旧缓存控制”两条链
- 未解风险 / 下一步：
  - 需要把这轮修复继续推到远端并等待 Pages 重建
  - 推送后复核线上首页是否恢复完整首屏；若仍异常，再继续看 CSS / 布局层

### 2026-04-16 第九轮

- 本轮目标：确认线上“只剩日期”的症状是否来自本地构建缺失，并彻底移除仍在发布产物中生效的旧缓存 Worker
- 已完成：
  - 直接复核本地 `site/dist/index.html`，确认首页正文完整存在，包含“唐山海钓指南 / 进入海岸钓 / 进入出海钓”，排除“本地首页空白”假设
  - 识别出生成产物中的 `sw.js` 仍是整站预缓存版本，会持续维持旧缓存壳
  - 将 `sw.js` 改为清缓存 + 通知客户端 + 自注销版本，不再预缓存整站资源
  - 移除对旧缓存 Worker 的静态烟雾测试盲区，新增断言：禁止 `PRECACHE_URLS` / `CACHE_NAME`，并要求 `self.registration.unregister()`
  - 顺序重跑 `npm run safety:preflight`
  - 顺序重跑 `npm run test:smoke`
- 已吸收结论：
  - 只靠运行时 `app.js` 注销旧 Worker 不够；如果旧 Worker 先接管入口，必须从发布产物层面替换 `sw.js`
  - 对 GitHub Pages + Safari/PWA 场景，静态验收必须覆盖 Worker 内容本身，不能只验 HTML
  - 大南阶段门槛要同时检查：构建结果完整、缓存接管已解除、静态回归通过
- 未解风险 / 下一步：
  - 需要把这轮 `sw.js` 修复推到远端
  - 推送后等待 GitHub Pages 重新部署，再复核线上首页是否恢复完整首屏

### 2026-04-16 第十轮

- 本轮目标：在不依赖外链样式和脚本的前提下，确保首页本身就能稳定显示“唐山海钓指南”入口
- 已完成：
  - 将首页改为独立内联版本：内联关键样式，直接渲染整理时间，不再依赖 `site.css` 和 `app.js`
  - 保留首页的核心结构：标题、模式卡、模式入口、排名说明、合规说明、最近更新
  - 复核生成后的 `site/dist/index.html`，确认首页 HTML 内直接包含“唐山海钓指南 / 进入海岸钓 / 进入出海钓”
  - 顺序复核 `npm run safety:preflight`
  - 顺序复核 `npm run test:smoke`
- 已吸收结论：
  - 首页必须是最稳的一层，不能再依赖运行时脚本或公共样式链路才能显现
  - 当线上表现与本地构建不一致时，第一层页面应优先退回到“静态可见、零依赖”的最保守方案
  - 大南验收对首页的要求应单独提高：即使 JS、SW 或样式链路异常，首页也要保持为可见的导航入口
- 未解风险 / 下一步：
  - 需要把这轮首页独立化修复推到远端
  - 推送后重新等待 GitHub Pages 部署，并复核线上首页是否恢复完整可见

### 2026-04-16 第十一轮

- 本轮目标：对三层级文案定稿做一次交付前 Gate
- 执行方式：
  - 大南线程在当前时限内未返回
  - 按既定协议由主线程代行大南 Gate，不跳过验收
- 已完成：
  - 复核 `/docs/level-1-home-copy-final-v1.md`
  - 复核 `/docs/level-2-list-copy-final-v1.md`
  - 复核 `/docs/level-3-detail-copy-final-v1.md`
- 验收结论：
  - 第一层级符合“分流”标准
  - 第二层级符合“比较”标准
  - 第三层级符合“决策”标准
  - 当前文案可以进入页面实现阶段
- 未解风险 / 下一步：
  - 进入页面实现后，还要继续验“有没有被实现跑偏”，不能只验文案本身

### 2026-04-16 第十二轮

- 本轮目标：对三层级页面实现做一次交付前 Gate
- 执行方式：
  - 大南线程在当前时限内未返回
  - 按既定协议由主线程代行大南 Gate，不跳过验收
- 已完成：
  - 复核首页是否只承担分流职责
  - 复核第二层级是否是比较页而非详情页
  - 复核第三层级是否是单点决策页
  - 顺序通过 `npm run safety:preflight`
  - 顺序通过 `npm run test:smoke`
- 验收结论：
  - 第一层级符合“分流”标准
  - 第二层级符合“比较”标准
  - 第三层级符合“决策”标准
- 当前页面实现可以进入用户查验阶段
- 未解风险 / 下一步：
  - 如果用户对三层级文案或信息顺序继续调整，需要再走一轮页面实现 + Danan Gate

### 2026-04-17 第十三轮

- 本轮目标：复核第二层级和第三层级的阶段性精修是否真正提升“比较 / 决策”效率
- 执行方式：
  - 大南线程返回高优先级问题清单
  - 主线程据此完成页面调整，再按既定 Gate 串行复核
- 已完成：
  - 确认第二层级新增显式“全部”筛选项
  - 确认第二层级新增新手 / 到达 / 成本的比较信号
  - 确认第三层级首屏新增“到达与准备”“首访建议”
  - 顺序通过 `npm run build`
  - 顺序通过 `npm run safety:preflight`
  - 顺序通过 `npm run test:smoke`
- 验收结论：
- 第二层级更接近比较页
- 第三层级首屏更接近单点决策页
- 当前实现可进入下一轮用户查验
- 未解风险 / 下一步：
  - 仍需在真实浏览器里继续看卡片留白、按钮层级和滚动节奏

### 2026-04-17 第十四轮

- 本轮目标：对继续精修后的第二层级和第三层级做一次串行 Gate
- 已完成：
  - 复核第二层级“全部”筛选、排序提示、比较卡是否仍然成立
  - 复核第三层级决策提示条与摘要卡顺序
  - 串行通过 `npm run build`
  - 串行通过 `npm run safety:preflight`
  - 串行通过 `npm run test:smoke`
- 验收结论：
  - 第二层级保持比较页定位
  - 第三层级首屏的决策效率继续提升
  - 当前版本可继续进入用户查验

### 2026-04-17 第十五轮

- 本轮目标：复核“分梯队排序页 + 首屏怎么钓 + 底部动作区”这一轮结构调整
- 已完成：
  - 复核第二层级新增梯队标题和“适合谁先看”比较字段
  - 复核第三层级新增“怎么钓”摘要项
  - 复核第三层级底部动作区文案与去向
  - 串行通过 `npm run build`
  - 串行通过 `npm run safety:preflight`
  - 串行通过 `npm run test:smoke`
- 验收结论：
  - 第二层级更接近推荐排序页而不是普通卡片流
  - 第三层级首屏开始覆盖“值不值得去 / 怎么去 / 怎么钓”
  - 当前版本可继续进入下一轮用户查验

### 2026-04-17 第十六轮

- 本轮目标：对首页重写后的“出发入口页”做一次串行 Gate
- 已完成：
  - 复核首页是否删除旧的解释型废话
  - 复核首页是否只保留两张主入口卡
  - 复核首页鱼种行、轻辅助入口与行动按钮
  - 串行通过 `npm run build`
  - 串行通过 `npm run safety:preflight`
  - 串行通过 `npm run test:smoke`
- 验收结论：
  - 第一层级已经回到入口页定位
  - 首页行动感提升，且未引入虚假承诺
  - 当前版本可继续进入下一轮用户查验
